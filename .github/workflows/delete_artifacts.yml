name: 'artifacts_cleanup'
on:
  push:
    branches: [ master ]

jobs:
  delete-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: kolpav/purge-artifacts-action@v1
        with:
          token: ${{ secrets.for_webhooks }}
          expire-in: 0 # Set this to 0 to delete all artifacts
